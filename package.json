{
  "name": "fairdirection-saas",
  "version": "1.0.0",
  "description": "Root management for Fairdirection SaaS",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev --prefix salesflow-backend\" \"npm run dev --prefix salesflow-frontend\"",
    "install:all": "npm install --prefix salesflow-backend && npm install --prefix salesflow-frontend",
    "backend": "npm run dev --prefix salesflow-backend",
    "frontend": "npm run dev --prefix salesflow-frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}